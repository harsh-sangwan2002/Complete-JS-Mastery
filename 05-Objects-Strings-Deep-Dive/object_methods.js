let str = "   asdjh "

// trim to clear out the spaces at the start and tail
console.log(str.trim());

// split the string based on some character -> converts into array
str = "I am a quick learner"
let arrStr = str.split(" ");
console.log(arrStr);

// join to join the string from array based on some character
console.log(arrStr.join(" "));